// WAP to print first 10 natural number in reverse order
// start = 10, stop = 1, gap = -1

#include <stdio.h>
int main()
{
    // initialization
    int i = 10;

    while (i >= 1)
    {
        printf("%d\n", i);

        // Decrement
        i--;
    }
}